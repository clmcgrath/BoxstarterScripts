{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "PowerShell",
            "request": "launch",
            "name": "PowerShell Launch (Script)",
            "script": "${workspaceRoot}/Install.ps1",
            "args": ["-ConfigPath ./Test.xml"],
            "cwd": "${workspaceRoot}"
        }
    ]
}